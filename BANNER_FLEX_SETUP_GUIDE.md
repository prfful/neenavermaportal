# बेनर फलेक्स फोटो सिस्टम - सेटअप गाइड

## 📋 सारांश

"बेनर फलेक्स फोटो" (Banner Flex Photo) सिस्टम एक नई सुविधा है जो उच्च परिभाषा फोटो को संग्रहीत करने और प्रदर्शित करने के लिए है जो बेनर और फलेक्स प्रिंटिंग के लिए उपयुक्त हैं।

## 🎯 मुख्य विशेषताएं

### 1. **मुख्य पृष्ठ पर नया लिंक**
- मुख्य नेविगेशन में "🖼️ बेनर फलेक्‍ स के लिये फोटो" लिंक जोड़ा गया है
- यह "📸 डाउनलोड गैलरी" के ठीक बाद दिखाई देता है
- डेस्कटॉप और मोबाइल दोनों में काम करता है

### 2. **नया बेनर फोटो पृष्ठ**
- **फाइल**: `banner-flex-photo.php`
- उच्च परिभाषा बेनर फोटो का एक संपूर्ण गैलरी
- **विशेषताएं**:
  - 📸 फोटो ग्रिड लेआउट
  - 🔍 खोज कार्यक्षमता
  - 📅 तारीख के अनुसार छंटना
  - 💾 व्यक्तिगत डाउनलोड
  - 🖼️ लाइटबॉक्स व्यू

### 3. **एडमिन अपलोड पैनल में नई सुविधा**
- **फाइल**: `photo-admin-upload.php`
- प्रमुख अपडेट:
  - पृष्ठ के निचले भाग में नया "बेनर/फलेक्स फोटो अपलोड" सेक्शन
  - आयाम विकल्प (लैंडस्केप, पोर्ट्रेट, बेनर, फलेक्स, आदि)
  - अधिक फाइल साइज समर्थन (10MB तक)
  - नई ड्रैग-ड्रॉप इंटरफेस

### 4. **डेटाबेस संशोधन**
- **नई कॉलम**: `is_banner_photo` (download_gallery_photos में)
- **नई टेबल**: `banner_photo_uploads` (ट्रैकिंग के लिए)
- फोटो को बेनर फोटो के रूप में चिह्नित करने की क्षमता

## 🔧 इंस्टॉलेशन चरण

### चरण 1: डेटाबेस स्कीमा अपडेट करें

अपने फिलाहाल इस्तेमाल करें:
```SQL फिलाहाल``` से निम्न फाइल को चलाएं:

```
database/add_banner_photos_schema.sql
```

**phpMyAdmin या कमांड लाइन से**:
```bash
mysql -h localhost -u your_username -p your_database_name < database/add_banner_photos_schema.sql
```

### चरण 2: निर्देशिका बनाएं

ये निर्देशिकाएं स्वचालित रूप से बनाई जाएंगी, लेकिन आप पहले से भी बना सकते हैं:

```
uploads/banner_photos/
uploads/banner_photos/2024-01/
uploads/banner_photos/2024-02/
```

**अनुमतियां सेट करें** (Linux/Mac):
```bash
chmod -R 755 uploads/banner_photos/
```

### चरण 3: फाइलें सत्यापित करें

सुनिश्चित करें कि ये फाइलें मौजूद हैं:
- ✅ `banner-flex-photo.php` - मुख्य गैलरी पृष्ठ
- ✅ `photo-admin-upload.php` - संशोधित (बेनर सेक्शन के साथ)
- ✅ `backend/upload-banner-photos-handler.php` - अपलोड हैंडलर
- ✅ `backend/fetch-banner-data.php` - डेटा फेचिंग स्क्रिप्ट
- ✅ `includes/header.php` - संशोधित (नेविगेशन लिंक के साथ)

## 📖 उपयोग कैसे करें

### एडमिन के लिए - बेनर फोटो अपलोड करना

1. `photo-admin-upload.php` पर जाएं
2. पृष्ठ के नीचे "🖼️ बेनर एवं फलेक्स फोटो अपलोड" सेक्शन तक स्क्रॉल करें
3. निम्नलिखित विवरण भरें:
   - **कार्यक्रम/इवेंट का नाम** (आवश्यक)
   - **कार्यक्रम की तारीख** (आवश्यक)
   - **कार्यक्रम का स्थान** (वैकल्पिक)
   - **फोटो का विवरण** (वैकल्पिक)
   - **फोटो का आयाम/प्रकार** (ड्रॉपडाउन से चुनें)

4. फोटो अपलोड करें:
   - क्लिक करके फाइलें चुनें
   - या ड्रैग-ड्रॉप करें
   - एक बार में कई फोटो अपलोड कर सकते हैं

5. "🖼️ बेनर फोटो अपलोड करें" बटन पर क्लिक करें

6. अपलोड सफल होने के बाद, आप `banner-flex-photo.php` पर फोटो देख सकते हैं

### जनता के लिए - बेनर फोटो देखना

1. मुख्य पृष्ठ पर नेविगेशन मेनू में "🖼️ बेनर फलेक्‍ स के लिये फोटो" क्लिक करें
2. सभी बेनर फोटो एक ग्रिड में दिखाई देंगे
3. फोटो पर क्लिक करके बड़ा देखें या डाउनलोड करें
4. खोज सुविधा का उपयोग करके फोटो खोजें
5. तारीख के अनुसार छंटना करें

## 🗂️ फाइल संरचना

```
uploads/
├── banner_photos/              [नई - बेनर फोटो के लिए]
│   ├── 2024-01/
│   │   ├── Event_Name_1/
│   │   │   ├── 1693456789_uuid.jpg
│   │   │   └── 1693456790_uuid.jpg
│   │   └── Event_Name_2/
│   └── 2024-02/
└── download_gallery/           [मौजूदा - नियमित गैलरी फोटो]
```

## 💾 डेटाबेस स्कीमा

### संशोधन: `download_gallery_photos` तालिका में

```sql
-- नई कॉलम
ALTER TABLE download_gallery_photos 
ADD COLUMN is_banner_photo tinyint(1) DEFAULT 0;

-- नई इंडेक्स
CREATE INDEX idx_is_banner_photo 
ON download_gallery_photos (is_banner_photo);
```

### नई तालिका: `banner_photo_uploads`

```sql
CREATE TABLE banner_photo_uploads (
  id int(11) AUTO_INCREMENT PRIMARY KEY,
  photo_id int(11) UNIQUE NOT NULL,
  admin_id int(11) DEFAULT NULL,
  uploaded_at timestamp DEFAULT CURRENT_TIMESTAMP,
  description text,
  dimensions_desc varchar(100),
  FOREIGN KEY (photo_id) REFERENCES download_gallery_photos(id)
);
```

## 🔐 सुरक्षा विशेषताएं

- ✅ एडमिन लॉगिन आवश्यक (सेशन जांच)
- ✅ फाइल प्रकार सत्यापन (केवल JPG, PNG, WEBP अनुमति)
- ✅ फाइल साइज सीमा (10MB तक)
- ✅ अद्वितीय फाइल नाम (timestamp + UUID)
- ✅ इनपुट सेनिटाइजेशन
- ✅ गतिविधि लॉगिंग

## 🧪 परीक्षण करें

### डेटाबेस जांचें:
```sql
-- कॉलम जांचें
DESCRIBE download_gallery_photos;
-- आउटपुट में `is_banner_photo` दिखना चाहिए

-- तालिका जांचें
DESCRIBE banner_photo_uploads;
```

### फोटो अपलोड करें:
1. एडमिन पैनल में लॉगिन करें
2. `photo-admin-upload.php` पर जाएं
3. बेनर सेक्शन में कुछ टेस्ट फोटो अपलोड करें
4. सफलता संदेश जांचें

### गेलरी देखें:
1. `banner-flex-photo.php` पर जाएं
2. अपलोड किए गए फोटो प्रदर्शित होने चाहिए
3. डाउनलोड कार्यक्षमता परीक्षण करें

## ⚙️ कॉन्फ़िगरेशन विकल्प

### फाइल साइज सीमा बदलें
`backend/upload-banner-photos-handler.php` में:
```php
$max_file_size = 10 * 1024 * 1024; // 10MB को अपनी आवश्यकता अनुसार बदलें
```

### डिलीट डेट बदलें
बेनर फोटो डिफ़ॉल्ट रूप से 30 दिन बाद स्वचालित रूप से हटा दिए जाते हैं:
```php
$deleteDateObj->modify('+30 days'); // दिनों की संख्या बदलें
```

## 📝 लॉग फाइलें

अपलोड की समस्याओं के लिए लॉग देखें:
```
uploads/upload_debug.log
```

## 🆘 समस्या निवारण

| समस्या | समाधान |
|--------|--------|
| फोटो अपलोड नहीं हो रहे | डेटाबेस स्कीमा अपडेट जांचें, निर्देशिका अनुमतियां जांचें |
| बेनर पृष्ठ सफेद दिखता है | डेटाबेस कनेक्शन जांचें, लॉग फाइलें देखें |
| डाउनलोड काम नहीं कर रहा | `backend/download-photo.php` से अनुमतियां जांचें |
| फोटो दिखाई नहीं दे रहे | `is_banner_photo = 1` सेट है जांचें |

## 📞 संपर्क करें

किसी समस्या के लिए:
1. `uploads/upload_debug.log` जांचें
2. डेटाबेस टेबल्स `SELECT COUNT(*) FROM download_gallery_photos WHERE is_banner_photo = 1;`
3. फाइल अनुमतियां जांचें

---

**संस्करण**: 1.0  
**तारीख**: 21 फरवरी 2026  
**अंतिम अपडेट**: 2026-02-21
